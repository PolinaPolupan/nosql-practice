services:
  redis:
    image: redis:8-alpine
    restart: unless-stopped
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
  cassandra:
    image: cassandra:latest
    ports:
      - "9042:9042"
  mongo:
    image: mongo
    ports:
      - "27017:27017"

volumes:
  redis_data: